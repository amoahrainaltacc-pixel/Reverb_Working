# nixpacks.toml

[phases.setup]
nixPkgs = ["python313", "ffmpeg", "libopus", "libsodium", "deno"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "python3 main.py"
